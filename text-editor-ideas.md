* auto repl (i.e. it always displays next to code while being written)
* syntax aware mode!
    * start renaming a variable, and it will change everywhere
    * change a function call structure, and it will be modified everywhere
